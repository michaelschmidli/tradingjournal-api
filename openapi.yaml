openapi: 3.1.0
info:
  title: 91 Tradingjournal Export API
  version: 1.0.0
servers:
  - url: https://tradingjournal-api.onrender.com
paths:
  /health:
    get:
      operationId: health
      summary: Health check
      responses:
        '200':
          description: OK
  /create-export:
    post:
      operationId: createExport
      summary: Erzeugt JSON und PDF, speichert beides in Dropbox und gibt Download-Links zurück
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - api_key
                - trade
              properties:
                api_key:
                  type: string
                  description: Geheimer API-Key für das Backend
                trade:
                  type: object
                  description: Vollständiges Trade-Objekt im Unified-Format
                  additionalProperties: true
      responses:
        '200':
          description: Export erfolgreich
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  success:
                    type: boolean
                  trade_id:
                    type: string
                  json_file:
                    type: object
                    additionalProperties: false
                    properties:
                      path:
                        type: string
                      url:
                        type: string
                  pdf_file:
                    type: object
                    additionalProperties: false
                    properties:
                      path:
                        type: string
                      url:
                        type: string
